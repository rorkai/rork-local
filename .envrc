if has nix; then
  watch_file bun.lock
  # `use flake` only tracks flake.nix/flake.lock, so the modules imported from
  # nix/ would otherwise keep a stale dev shell after an edit.
  watch_dir nix
  use flake
fi
